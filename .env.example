# Server only. Add your own key locally; never use a VITE_* variable for it.
GEMINI_API_KEY=
# Blank uses gemini-2.5-flash. Choose a vision + structured-output capable model.
GEMINI_MODEL=gemini-2.5-flash
# Optional local backend port (Vite proxy uses this same value).
PORT=3001
